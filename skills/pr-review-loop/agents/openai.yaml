interface:
  display_name: "PR Review Loop"
  short_description: "Resolve stack feedback until Codex reviews clean"
  default_prompt: "Use $pr-review-loop on the current PR stack, applying my run conditions and tailored Codex review instructions until every current head receives a clean review."
policy:
  allow_implicit_invocation: false
