interface:
  display_name: "Setup Engineering Workflow"
  short_description: "Configure trackers, labels, and domain docs"
  default_prompt: "Use $setup-engineering-workflow to configure this repository for the spec, ticket, and implementation workflows."
policy:
  allow_implicit_invocation: false
