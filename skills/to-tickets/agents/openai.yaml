interface:
  display_name: "To Tickets"
  short_description: "Split work into dependency-aware slices"
  default_prompt: "Use $to-tickets to break this plan into dependency-aware vertical slices."
policy:
  allow_implicit_invocation: false
